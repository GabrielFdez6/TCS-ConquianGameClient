<Window x:Class="ConquiánCliente.View.MainMenu.ChangeLanguage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:language="clr-namespace:ConquiánCliente.Properties.Langs"
        Title="{x:Static language:Lang.GlobalLanguage}"
        Height="400" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">

    <Grid>
        <Border Background="{StaticResource PrimaryBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="2"
                CornerRadius="10"
                Padding="40"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect Color="{Binding Color, Source={StaticResource BorderBrush}}" 
                                  ShadowDepth="0" 
                                  BlurRadius="20" 
                                  Opacity="1"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Background="Transparent">

                <Button Style="{StaticResource PrimaryButtonStyle}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,-30,-30,10"
                        Content="X"
                        Width="30"
                        Height="30"
                        FontWeight="Bold"
                        Click="CloseButton_Click"/>

                <Button Width="300" Height="80" Margin="0,10,0,10"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="ClickSpanish">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image Source="/Resources/MexicoFlag.png"
                               Width="150" Height="70"/>
                        <TextBlock Text="{x:Static language:Lang.es}"
                                   FontSize="26"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Width="300" Height="80" Margin="0,10,0,10"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="ClickEnglish">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image Source="/Resources/USAFlag.png"
                               Width="150" Height="70"/>
                        <TextBlock Text="{x:Static language:Lang.en}"
                                   FontSize="26"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>