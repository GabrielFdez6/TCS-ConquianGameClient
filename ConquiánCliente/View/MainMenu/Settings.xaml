<Window x:Class="ConquiánCliente.View.MainMenu.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:language="clr-namespace:ConquiánCliente.Properties.Langs"
        Title="{x:Static language:Lang.MainMenuSettings}"
        Height="550" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">

    <Grid>
        <Border Background="{StaticResource PrimaryBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="2"
                CornerRadius="15"
                Padding="30">

            <Border.Effect>
                <DropShadowEffect Color="{Binding Color, Source={StaticResource BorderBrush}}" 
                                  ShadowDepth="0" 
                                  BlurRadius="20" 
                                  Opacity="1"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0"
                        Style="{StaticResource PrimaryButtonStyle}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,-15,-15,10"
                        Content="X"
                        Width="35"
                        Height="35"
                        FontWeight="Bold"
                        FontSize="16"
                        Click="CloseButtonClick"/>

                <TextBlock Grid.Row="1" 
                           Text="{x:Static language:Lang.MainMenuMusic}"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           FontSize="24"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,15"/>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Width="300">
                    <TextBlock Text="🔈" FontSize="20" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <Slider x:Name="VolumeSlider"
                            Width="250"
                            Minimum="0" 
                            Maximum="100" 
                            TickFrequency="10" 
                            IsSnapToTickEnabled="True"
                            ValueChanged="VolumeSliderValueChanged"
                            VerticalAlignment="Center"/>

                    <TextBlock Text="🔊" FontSize="20" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <Separator Grid.Row="3" 
                           Background="{StaticResource BorderBrush}" 
                           Margin="20,30,20,30" 
                           Opacity="0.5"/>

                <TextBlock Grid.Row="4" 
                           Text="{x:Static language:Lang.GlobalLanguage}"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           FontSize="24"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,15"/>

                <StackPanel Grid.Row="5" VerticalAlignment="Top" HorizontalAlignment="Center">
                    <Button Width="320" Height="70" Margin="0,0,0,15"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="ClickSpanish">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Image Source="/Resources/MexicoFlag.png" Width="80" Margin="0,0,15,0"/>
                            <TextBlock Text="{x:Static language:Lang.es}"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Width="320" Height="70"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="ClickEnglish">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Image Source="/Resources/USAFlag.png" Width="80" Margin="0,0,15,0"/>
                            <TextBlock Text="{x:Static language:Lang.en}"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>