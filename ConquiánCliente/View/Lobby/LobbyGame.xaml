<Window x:Class="ConquiánCliente.View.Lobby.LobbyGame"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:language="clr-namespace:ConquiánCliente.Properties.Langs"
    mc:Ignorable="d"
    Title="{x:Static language:Lang.Lobby}"
    Height="1080" Width="1920"
    WindowState="Maximized"
    WindowStartupLocation="CenterScreen"
    Background="Transparent">

    <Grid>
        <Image Source="/Resources/backgroundMainMenu.png" Stretch="UniformToFill"/>
        <Grid>
            <Button
                HorizontalAlignment="Left" VerticalAlignment="Top"
                Margin="20" Width="50" Height="50"
                Content="&lt;" FontSize="24" FontWeight="Bold"
                Style="{StaticResource PrimaryButtonStyle}"
                Command="{Binding GoBackCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                <Button.Effect>
                    <DropShadowEffect Color="#A43C42" BlurRadius="10" ShadowDepth="2" Opacity="1"/>
                </Button.Effect>
            </Button>

            <StackPanel Orientation="Vertical"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Margin="20">
                <TextBlock Text="{x:Static language:Lang.LobbyRoomCode}"
                           FontSize="22" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Right"/>
                <TextBlock
                    Text="{Binding RoomCode}" FontSize="28" FontWeight="Bold"
                    Foreground="{StaticResource PrimaryTextBrush}"
                    HorizontalAlignment="Right" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Orientation="Vertical"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="20,100,0,0">

                <ItemsControl ItemsSource="{Binding Players}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <Border Width="70" Height="70" BorderBrush="Gray" BorderThickness="1" Background="Transparent">
                                    <Image Source="{Binding ProfileImagePath}" />
                                </Border>
                                <TextBlock Text="{Binding DisplayName}"
                                           VerticalAlignment="Center"
                                           Margin="10,0,0,0"
                                           FontSize="22"
                                           Foreground="White"
                                           FontWeight="Bold"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
            <StackPanel VerticalAlignment="Top"
                        HorizontalAlignment="Center"
                        Margin="0,60,0,0">
                <TextBlock Text="{x:Static language:Lang.Lobby}"
                           FontSize="70"
                           FontWeight="Bold"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding PlayerCountText}"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
                <Button Width="388" Height="62" Margin="0,20,0,0" Style="{StaticResource PrimaryButtonStyle}">
                    <Button.Effect>
                        <DropShadowEffect Color="#A43C42" BlurRadius="10" ShadowDepth="2" Opacity="1"/>
                    </Button.Effect>
                    <TextBlock Text="{x:Static language:Lang.LobbyStartGame}"
                               FontSize="26"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <Button Width="60" Height="50" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding PreviousGameTypeCommand}">
                        <Button.Effect>
                            <DropShadowEffect Color="#A43C42" BlurRadius="10" ShadowDepth="2" Opacity="1"/>
                        </Button.Effect>
                        <TextBlock Text="&lt;" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}"/>
                    </Button>

                    <Label Width="200" Height="50" Margin="10,0,10,0" Background="#F2A85F"
                           HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                        <TextBlock Text="{Binding SelectedGameType}"
                                   FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}"/>
                    </Label>

                    <Button Width="60" Height="50" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding NextGameTypeCommand}">
                        <Button.Effect>
                            <DropShadowEffect Color="#A43C42" BlurRadius="10" ShadowDepth="2" Opacity="1"/>
                        </Button.Effect>
                        <TextBlock Text=">" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}"/>
                    </Button>
                </StackPanel>

                <Button Width="388" Height="62" Margin="0,30,0,0" Style="{StaticResource PrimaryButtonStyle}">
                    <Button.Effect>
                        <DropShadowEffect Color="#A43C42" BlurRadius="10" ShadowDepth="2" Opacity="1"/>
                    </Button.Effect>
                    <TextBlock Text="{x:Static language:Lang.GlobalInvite}"
                               FontSize="26"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Button>
            </StackPanel>

            <Grid HorizontalAlignment="Left"
                  VerticalAlignment="Bottom"
                  Margin="20,0,0,48"
                  Width="500"
                  Height="200">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Background="#66000000" Padding="5">
                    <ItemsControl ItemsSource="{Binding ChatMessages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Foreground="White" FontSize="16" Margin="0,2" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <TextBox Grid.Row="1" Height="40"
                         Margin="0,5,0,0"
                         FontSize="16"
                         VerticalContentAlignment="Center"
                         Text="{Binding CurrentMessage, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SendMessageCommand}" />
                    </TextBox.InputBindings>
                </TextBox>
            </Grid>
        </Grid>
    </Grid>
</Window>