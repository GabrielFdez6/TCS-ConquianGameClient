<Window x:Class="ConquiánCliente.View.Utilities.ReconnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConquiánCliente.View.Utilities"
        xmlns:language="clr-namespace:ConquiánCliente.Properties.Langs"
        mc:Ignorable="d"
        Title="{x:Static language:Lang.GlobalReconnecting}" 
        WindowStyle="None" 
        ResizeMode="NoResize" 
        AllowsTransparency="True" 
        ShowInTaskbar="False"
        Background="Transparent"
        Topmost="True" 
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Border Background="{StaticResource PrimaryBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="2"
                CornerRadius="10"
                Padding="40"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect Color="{Binding Color, Source={StaticResource BorderBrush}}" 
                                  ShadowDepth="0" 
                                  BlurRadius="20" 
                                  Opacity="1"/>
            </Border.Effect>

            <StackPanel Width="360" Orientation="Vertical">
                <TextBlock Text="{x:Static language:Lang.TitleConnectionLost}" 
                           Style="{StaticResource TitleTextBlock}"
                           FontSize="26"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,0,20"/>

                <TextBlock Text="{x:Static language:Lang.TextPopUpReconecting}" 
                           Foreground="{StaticResource PrimaryTextBrush}"
                           FontSize="16" 
                           FontWeight="Normal"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,0,30"/>

                <ProgressBar IsIndeterminate="True" 
                             Height="10" 
                             Width="250" 
                             Margin="0,0,0,10"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>